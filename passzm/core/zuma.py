import core.io
import core.sorter as sorter
import core.transformer

#start
def spiral(rawtxt):
	#config, material = sorter.pickup(rawtxt)
	sorter.pickup(rawtxt)


class Zuma:

	def __init__(self, rawtxt):
		self.config, self.classify = sorter.pickup(tawtxt)

	def register(self, tactice):
		pass

